# Frontend Environment Variables

# Backend API URL
# For local development (with backend running locally):
# VITE_BACKEND_URL=http://localhost:8080
#
# For production deployment (nginx proxy handles routing):
VITE_BACKEND_URL=
